node_modules/
.env
.env.local
.env.production
*.log
nohup.out
dist/
.DS_Store
.netlify/

# Frontend source design assets (root-level only — production assets in /public/ kept)
/frontend/*.mp4
/frontend/*.zip
/frontend/*.docx
/frontend/*.pdf
/frontend/*.jpeg
/frontend/*.jpg
/frontend/_brand/
/frontend/_frames/
/frontend/_originals_backup/
/frontend/_founder.txt
/frontend/_changes.txt
/frontend/color-probe/
/.image-backups-cropped/

# frontend/ is now a local symlink to ~/primal-revamp (deployed via Netlify CLI).
# Don't track its contents in this repo — primal-revamp is the source of truth.
/frontend
/frontend.local-backup

